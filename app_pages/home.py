import streamlit as st  # type: ignore

    
# st.logo(page_logo)
st.title("Mind is not free, but use it anyway with :blue[LOGIC] :sunglasses:")
st.subheader("Langage is so confusing, use symbolic maths to solve logic problems")